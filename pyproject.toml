[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "miniagentlab"
version = "0.1.0"
description = "A lightweight agent orchestration playground."
requires-python = ">=3.10"
readme = "README.md"
dependencies = []

[tool.setuptools.packages.find]
include = ["miniagentlab*"]
